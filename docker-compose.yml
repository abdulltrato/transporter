services:
  postgres:
    image: postgres:16-alpine
    container_name: transporter-postgres
    environment:
      POSTGRES_DB: transporter
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5433:5432'
    volumes:
      - transporter_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d transporter']
      interval: 5s
      timeout: 5s
      retries: 20
    restart: unless-stopped

  redis:
    image: redis:8-alpine
    container_name: transporter-redis
    ports:
      - '6379:6379'
    command: ['redis-server', '--save', '""', '--appendonly', 'no']
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 20
    restart: unless-stopped

volumes:
  transporter_postgres_data:
